<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>たけうち百貨店 Online Shop</title>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
     integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link href='/stylesheets/style.css' rel='stylesheet' />
    <!-- <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script> -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
     integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
  </head>
  <body style="height: 100%; background-color: #EEE">
    <!-- <jsp:include page="body_header.jsp"/>  -->
    <div class="container-fluid">
      <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
          <div class="row">
            <div class="col-3">
              <!-- <jsp:include page="item_search.jsp"/> -->
            </div>
            <div class="col-9">
            <% for (var i = 0; i < content.length; i = i + 3 ) { %>
              <div class="card-group mb-2">
              <% for (var j = 0; j < 3; j++) { %>
                <% var obj = content[i+j]; %>
                <% if (obj.ITEMID == 0) { %>
                <div class="card invisible"></div>
                <% } else { %>
                <div class="card shadow-sm" style="width: 18rem;">
                  <div class="card-header text-center" style="height: 200px;">
                    <img src="/images/default.png" class="img-fluid mh-100">
                  </div>
                  <div class="card-body">
                    <h5 class="card-title"><%= obj.ITEMNAME %></h5>
                  </div>
                  <div class="card-footer">
                    <p class="card-text text-danger"><%= obj.PRICE %></p>
                    <form method="get" action="/itemdetail">
                    <p>
                      <button type="submit" class="btn btn-success w-100" name="itemId" value="<%= obj.ITEMID %>">
                      <i class="fas fa-search-plus"></i> 詳細</button>
                    </p>
                    </form>
                    <form method="get" action="/cart">
                    <p>
                      <input type = "hidden" name="amount" value="1">
                      <input type = "hidden" name="itemId" value="<%= obj.ITEMID %>">
                      <button type="submit" class="btn btn-warning w-100" name="action" value="add">
                        <i class="fa fa-cart-plus" aria-hidden="true"></i> カートに追加
                      </button>
                    </p>
	      	          </form>
                  </div>
                </div>
                <% } %>
              <% } %>
              </div>
            <% } %>
            </div>
          </div>
	      </div>
        <div class="col-md-1"></div>
	    </div>
	  </div>
  </body>
</html>
